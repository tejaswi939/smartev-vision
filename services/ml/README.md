# services/ml (placeholder)

Python FastAPI + scikit-learn analytics service. Built in **Phase 3** by lifting
`legacy/app/services/analytics.py`, `legacy/app/services/heatmap.py`, and `legacy/ml/`.

Inert in Phase 0 — this file only reserves the workspace slot so the monorepo layout
is stable. No code runs here yet.
