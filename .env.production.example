# Production environment secrets — copy to .env.production and fill in real values.
# DATABASE_URL and ML_SERVICE_URL are wired internally by docker-compose.prod.yml
# and do NOT need to be set here when using compose.

JWT_ACCESS_SECRET="<long-random-secret-change-me>"
JWT_REFRESH_SECRET="<different-long-random-secret-change-me>"
